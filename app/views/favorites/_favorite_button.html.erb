<% if current_user.favoriting?(comment) %>
    <%= form_with(model: current_user.favorites.find_by(comment_id: comment.id), local: true, method: :delete) do |f| %>
         <%= hidden_field_tag :comment_id, comment.id %>
         <%= f.submit 'いいねから取り消す', class: 'btn btn-secondary btn-sm' %>
    <% end %>
<% else %>
    <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
        <%= hidden_field_tag :comment_id, comment.id %>
        <%= f.submit 'いいね', class: 'btn btn-primary btn-sm' %>
    <% end %>
<% end %>   