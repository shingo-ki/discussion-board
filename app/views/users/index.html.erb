  <ul class="list-unstyled">
    <%= 'ユーザ一覧' %>
    <% @users.each do |user| %>
      <li class="media">
        <div class="media-body">
          <div>
            <p><%= link_to user.id , user_path(user) %> : <%= link_to user.name, user_path(user) %></p>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
  <%= paginate @users %>
